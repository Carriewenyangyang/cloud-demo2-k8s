apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: demo2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: yangyangwen/frontend:latest
        ports:
        - containerPort: 80